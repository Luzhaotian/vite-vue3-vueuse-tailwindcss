import Request from "@/apis/request.js";

export const downloadRequest = (url, data, method) => {
	// 调用接口
	// return Request({
	//     url: '/download',
	//     method: 'get',
	//     responseType: 'blob'
	// })
};
